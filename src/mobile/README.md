# Coffee Moodee Mobile Environment

This folder is for the mobile version of Coffee Moodee, built with Expo and React Native.

## Setup Instructions
1. Install Node.js and npm.
2. Run `npm install -g expo-cli` to install Expo CLI.
3. Initialize the project with `expo init mobile-app`.
4. Follow the frontend tech stack specification in docs/specifications/frontend.md.
